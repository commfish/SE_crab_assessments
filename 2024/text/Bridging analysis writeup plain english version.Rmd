---
title: "RTMB Juneau Bridging Analysis: A Clear and Simple Overview"
author: "Alex Reich"
date: "2025-06-27"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
library(knitr)
```


# Juneau SE RKC RTMB Bridging Analysis: Excel RSS CSA to RTMB likelihood CSA

This is a write up of the bridging analysis from the Excel residual sum of squares (RSS) catch-survey analysis (CSA) to the RTMB likelihood CSA. I re-ran the Juneau 2024 CSA analysis and compare the standardized CPUE and biomass outputs from both models. 

## Introduction
Excel, while a great and useful tool for simple data analysis and visualization, has a limited functionality for more complex stock assessments and is prone to user error. The prior Southeast Alaska Red King Crab (RKC) catch-survey analysis (CSA) was done in Excel using a RSS approach, involving a series manual steps for the program to calculate the CPUE and biomass estimates. The manual steps introduce the potential for user error and make the analysis less reproducible. Moving the analysis into R removes the manual steps by instead turning them into a series of code commands to be run, which reduces the potential for user error and makes the analysis more reproducible. 

Template Model Builder (TMB) is a model-building framework in C++ that can be used to create both simple and complex models (Kristensen et al 2016). R Template Model Builder (RTMB) is an R package that allows users to design TMB models using R instead of C++ (Kristensen 2024). Given that most biometricians and fisheries biologists are more comfortable in R than C++, using RTMB makes the model code more accessible and easier to understand. Some federal stock assessments use RTMB (Gulf of Alaska Northern Rockfish, Williams and Hulson 2024) or plan to use RTMB in the future (Eastern Bering Sea Pollock, Ianelli et al. 2024).

The RTMB likelihood CSA uses the same data and model structure as the Excel RSS CSA, but it is built in R using the RTMB package. The notable difference is that the RTMB likelihood CSA uses a negative log likelihood approach to estimate the parameters, while the Excel RSS CSA uses a residual sum of squares approach. The RTMB likelihood CSA also produces confidence intervals for the estimates, which the Excel RSS CSA does not. The following sections will compare the results from the RTMB likelihood CSA to the results from the Excel RSS CSA. The goal is to show that the two models produce similar results, while also highlighting the benefits of using RTMB likelihood over Excel RSS.

## CPUE

```{r CPUE, fig.cap = "Juneau RKC CPUE: RTMB likelihood (solid, with 95% confidence intervals) vs Excel residual sum of squares (dashed) results. The observed CPUE is depicted by the black points, with CV error bars based on the historic weighting system."}
#put image here
knitr::include_graphics('../figures/CSA_JNU_9_CPUE_grey.png', dpi = 125)


```


## Biomass

```{r Biomass, fig.cap = "Juneau RKC mature, legal, and pre-recruit biomass comparison between RTMB likelihood and Excel residual sum of squares methodology. The RTMB likelihood estimates have 95% confidence intervals."}
#put image here
knitr::include_graphics('../figures/CSA_JNU_9_Biomass_greyscale.png', dpi = 125)

```

## Differences between the two models
The two models are very similar. The effectively have the same fixed and estimated parameters and both assume normally distributed error. In this framework, the RTMB likelihood model will produce similar results to the Excel RSS model. The discrepancy in results is due to slight differences in the underlying mechanisms of Excel vs. R. RTMB also handles missing values differently than Excel. The main model difference is that the RTMB likelihood version has a confidence interval and it is easier to run (meaning less potential for biometrician mistakes).

### Statistical differences between the two models
The Excel RSS model uses residual sum of squares. This means that it is finding parameter estimates that make the difference between the observed values and predicted values as small as possible, aiming to minimize the error. The RTMB likelihood model uses negative log likelihood. It estimates the parameter values that maximize the likelihood (or minimize the negative log likelihood). In plain English, it selects parameters that makes the observed data most plausible under the defined model.

## Reasons for switching to the RTMB likelihood CSA
- R is a more powerful and reproducible analysis template compared to Excel. The potential for user error is reduced.
- The RTMB likelihood analysis produces confidence intervals, which the Excel analysis does not. Confidence intervals are
 useful for understanding the uncertainty in the point estimates.
- The RTMB likelihood model can be made more complex. We can add more robustness to this model in the future if we choose to do so.

## What now?
I plan to run both models for the Juneau Area. This will allow you to look at the results from both model versions when deciding the personal use opener for the Juneau Area personal use RKC fishery. The results from both models should be similar. 

# References
Kristensen, K., Nielsen, A., Berg, C.W., Skaug, H.J., Bell, B.M., and Kristensen, K. (2016). TMB: Automatic Differentiation and Laplace Approximation. Journal of Statistical Software, 70(5), 1-21. doi:10.18637/jss.v070.i05.

Kristensen, K. (2024). RTMB: 'R' Bindings for 'TMB.' https://CRAN.R-project.org/package=RTMB.




